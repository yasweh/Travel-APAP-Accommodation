apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: accommodation-fe-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: 2306212083-fe.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: accommodation-fe-service
                port:
                  number: 80
